name: worklifebell_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  flutter_bloc: ^7.0.1
  rxdart: ^0.27.1
  equatable: ^2.0.2
  path_provider: ^2.0.2
  location_permissions: ^4.0.0
  background_locator: ^1.6.2+1-beta

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
